# Receipt Manager Configuration
# Copy to .env and configure the required settings

# ============================================
# REQUIRED - You must set these
# ============================================

# n8n API Key (get from: http://localhost:5678 → Settings → n8n API → Create API Key)
N8N_API_KEY=

# Ollama VLM model for receipt processing (any vision-capable model works)
# Recommended: qwen3-vl:8b-thinking-q8_0 (great lightweight reasoning + vision capabilities)
# Run: ollama pull <model-name> to download first
OLLAMA_VLM_MODEL=qwen3-vl:8b-thinking-q8_0

# ============================================
# OPTIONAL - Defaults work for most users
# ============================================

# Database credentials
POSTGRES_DB=receipt_manager
POSTGRES_USER=receipt_user
POSTGRES_PASSWORD=receipt_password
POSTGRES_PORT=5432

# Service ports
WEB_PORT=8080
N8N_PORT=5678

# Ollama API endpoint (default: host machine)
VLM_API_URL=http://host.docker.internal:11434
